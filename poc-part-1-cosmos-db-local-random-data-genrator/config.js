var config = {};

config.endpoint = "https://localhost:8081";
config.key = "xxxx";

config.database = {
  id: "querytoblobpoc",
};

config.container = {
  id: "querytoblobpoc",
};

module.exports = config;
